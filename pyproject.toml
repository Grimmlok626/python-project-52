[project]
name = "hexlet-code"
version = "0.1.0"
dependencies = [
    "django",
    "uv",
    "gunicorn",
    "dj-database-url",
    "python-dotenv",
    "django-bootstrap5",
    "psycopg2-binary",
]

[tool.hatch.envs.default]
python = ">=3.10"